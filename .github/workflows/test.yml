name: test
on: push

jobs:
    job_1:
        name: First Job
        runs-on: ubuntu-latest
        steps:
            - name: Step one
              uses: actions/checkout@v2
            - name: Step two
              run: env | sort
            - name: Step three
              run: docker build -t demo-spring:latest .
            - name: Step four
              run: docker run --name demo-spring -p 8080:8080 -d demo-spring
              
              
             
         
        
